# Portal accounts (EXAMPLE — never commit the real one)

> ⚠️ Copy to `credentials.md`, which is **gitignored**. The real file holds passwords and MUST NOT
> be committed or shared. Prefer a password manager; this file is a convenience index only.
> Use a per-candidate password that does NOT contain their first name (several ATSs reject that).

| Portal | URL | Username | Password | Status / notes |
|---|---|---|---|---|
| Example ATS (Workday) | example.myworkdayjobs.com | candidate@email.com | (in password manager) | account created + email verified |
