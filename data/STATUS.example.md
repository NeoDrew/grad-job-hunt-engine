# Status (optional)

A human-readable running status of the search: hottest leads, upcoming interviews, what's
waiting on the candidate. The sweep also scans this file (if present) for already-engaged
employers when deciding what is "new to us". Copy to STATUS.md and keep it current, or delete
it — it is optional.
